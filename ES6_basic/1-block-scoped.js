export default function taskBlock (trueOrFalse) {
  const task = false;
  const task2 = true;

  if (trueOrFalse) {
    const innerTask = true;
    const innerTask2 = false;
    // You can use innerTask and innerTask2 here if needed
  }

  return [task, task2];
}
